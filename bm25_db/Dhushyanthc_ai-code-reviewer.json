{"corpus": ["async def generate_code_review(diff:str) -> str:\n  \"\"\"\n  Send the pr diff to GEMINI and returns a structured code review\n\n  diff: unified diff string from GitHub API\n  return: code review string generated by Gemini(formatted according to the system prompt)\n  \"\"\"\n\n  prompt = f\"{system_prompt}\\n\\nHere is the diff:\\n{diff}\"\n\n  response = model.generate_content(prompt)\n\n  return response.text", "async def fetch_pr_diff(repo_full_name: str, pr_number: int) -> str:\n  \"\"\"\n  Fetches the unified diff for a pull request.\n  Returns the raw diff as a string.\n\n  repo_full_name: \"owner/repo\"\n  pr_number: 42\n  \"\"\"\n\n  url = f\"{GITHUB_API_BASE}/repos/{repo_full_name}/pulls/{pr_number}\"\n\n  diff_header = {\n    **BASE_HEADERS,\n    \"Accept\": \"application/vnd.github.diff\"\n  }\n\n  async with httpx.AsyncClient() as client:\n    response = await client.get(url, headers=diff_header)\n\n    response.raise_for_status()\n\n    return response.text", "async def post_pr_comment(repo_full_name: str, pr_number: int, comment: str) -> None:\n    \"\"\"\n    Posts a comment on the pull request.\n\n    repo_full_name: \"owner/repo\"\n    pr_number: 42\n    body: markdown string - Gemini's code review\n    \"\"\"\n\n    url = f\"{GITHUB_API_BASE}/repos/{repo_full_name}/issues/{pr_number}/comments\"\n\n    async with httpx.AsyncClient() as client: \n      response = await client.post(\n        url,\n        headers = BASE_HEADERS,\n        json = {\"body\": comment}\n      )\n\n      response.raise_for_status()", "async def health_check():\n    return {\"status\": \"ok\"}", "async def webhook(\n    request: Request,\n    background_tasks: BackgroundTasks,\n    _: None = Depends(verify_signature),\n    x_github_event: Optional[str] = Header(None)\n):\n    # Handle ping \u2014 GitHub sends this when webhook is first registered\n    if x_github_event == \"ping\":\n        return {\"status\": \"pong\"}\n\n    # Ignore everything except pull_request events\n    if x_github_event != \"pull_request\":\n        return {\"status\": \"ignored\", \"event\": x_github_event}\n\n    # Safe to parse body now \u2014 we know it's a pull_request event\n    body = await request.json()\n\n    action = body.get(\"action\")\n    print(f\"[webhook] PR action received: {action}\")\n\n    if action not in [\"opened\", \"synchronize\"]:\n        return {\"status\": \"ignored\", \"action\": action}\n\n    repo_full_name = body[\"repository\"][\"full_name\"]\n    pr_number = body[\"pull_request\"][\"number\"]\n\n    print(f\"[webhook] Queuing review for PR #{pr_number} in {repo_full_name}\")\n    background_tasks.add_task(review_pr, repo_full_name, pr_number)\n\n    return {\"status\": \"accepted\", \"pr\": pr_number}\n\n\n#", "c def review_pr(repo_full_name: str, pr_number: int):\n    print(f\"[review_pr] Starting review \u2014 PR #{pr_number} in {repo_full_name}\")\n\n    try:\n        diff = await fetch_pr_diff(repo_full_name, pr_number)\n        print(f\"[review_pr] Diff fetched \u2014 {len(diff)} characters\")\n    except Exception as e:\n        print(f\"[review_pr] FAILED fetch_pr_diff \u2014 PR #{pr_number} \u2014 {e}\")\n        return\n\n    try:\n        review = await generate_code_review(diff)\n        print(f\"[review_pr] Review generated \u2014 {len(review)} characters\")\n    except Exception as e:\n        print(f\"[review_pr] FAILED generate_code_review \u2014 PR #{pr_number} \u2014 {e}\")\n        return\n\n    try:\n        await post_pr_comment(repo_full_name, pr_number, review)\n        print(f\"[review_pr] Done \u2014 comment posted to PR #{pr_number}\")\n    except Exception as e:\n        print(f\"[review_pr] FAILED post_pr_comment \u2014 PR #{pr_number} \u2014 {e}\")\n        return\n\n\n# --- Debug endpoint ", "quest: Request):\n    raw_body = await request.body()\n    print(f\"[debug] content-type: {request.headers.get('content-type')}\")\n    print(f\"[debug] body length: {len(raw_body)}\")\n    print(f\"[debug] raw body: {raw_body[:500]}\")\n    return {\"status\": \"ok\"}", "async def verify_signature(request: Request):\n  \"\"\"FastAPI dependency runs before the webhook handler.\n  Verifies the request is from the GitHub. \n  Return 401 if the signature is invalid or missing.\"\"\"\n\n  signature_header = request.headers.get(\"X-Hub-Signature-256\")\n\n  if not signature_header:\n    raise HTTPException(\n      status_code=401,\n      detail=\"Missing X-Hub-Signature-256 header\"\n    )\n  \n  body = await request.body()\n\n  expected_signature = \"sha256=\" + hmac.new(\n    key = WEBHOOK_SECRET.encode(\"utf-8\"),\n    msg = body,\n    digestmod = hashlib.sha256\n  ).hexdigest()\n\n  if not hmac.compare_digest(expected_signature, signature_header):\n    HTTPException(\n      status_code=401,\n      detail= \"Invalid signature - request not from GitHub\"\n    )"], "metadata": [{"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\gemini.py", "start_line": 39, "end_line": 51, "node_type": "function_definition", "content": "async def generate_code_review(diff:str) -> str:\n  \"\"\"\n  Send the pr diff to GEMINI and returns a structured code review\n\n  diff: unified diff string from GitHub API\n  return: code review string generated by Gemini(formatted according to the system prompt)\n  \"\"\"\n\n  prompt = f\"{system_prompt}\\n\\nHere is the diff:\\n{diff}\"\n\n  response = model.generate_content(prompt)\n\n  return response.text"}, {"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\github.py", "start_line": 15, "end_line": 36, "node_type": "function_definition", "content": "async def fetch_pr_diff(repo_full_name: str, pr_number: int) -> str:\n  \"\"\"\n  Fetches the unified diff for a pull request.\n  Returns the raw diff as a string.\n\n  repo_full_name: \"owner/repo\"\n  pr_number: 42\n  \"\"\"\n\n  url = f\"{GITHUB_API_BASE}/repos/{repo_full_name}/pulls/{pr_number}\"\n\n  diff_header = {\n    **BASE_HEADERS,\n    \"Accept\": \"application/vnd.github.diff\"\n  }\n\n  async with httpx.AsyncClient() as client:\n    response = await client.get(url, headers=diff_header)\n\n    response.raise_for_status()\n\n    return response.text"}, {"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\github.py", "start_line": 39, "end_line": 57, "node_type": "function_definition", "content": "async def post_pr_comment(repo_full_name: str, pr_number: int, comment: str) -> None:\n    \"\"\"\n    Posts a comment on the pull request.\n\n    repo_full_name: \"owner/repo\"\n    pr_number: 42\n    body: markdown string - Gemini's code review\n    \"\"\"\n\n    url = f\"{GITHUB_API_BASE}/repos/{repo_full_name}/issues/{pr_number}/comments\"\n\n    async with httpx.AsyncClient() as client: \n      response = await client.post(\n        url,\n        headers = BASE_HEADERS,\n        json = {\"body\": comment}\n      )\n\n      response.raise_for_status()"}, {"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\main.py", "start_line": 13, "end_line": 14, "node_type": "function_definition", "content": "async def health_check():\n    return {\"status\": \"ok\"}"}, {"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\main.py", "start_line": 19, "end_line": 48, "node_type": "function_definition", "content": "async def webhook(\n    request: Request,\n    background_tasks: BackgroundTasks,\n    _: None = Depends(verify_signature),\n    x_github_event: Optional[str] = Header(None)\n):\n    # Handle ping \u2014 GitHub sends this when webhook is first registered\n    if x_github_event == \"ping\":\n        return {\"status\": \"pong\"}\n\n    # Ignore everything except pull_request events\n    if x_github_event != \"pull_request\":\n        return {\"status\": \"ignored\", \"event\": x_github_event}\n\n    # Safe to parse body now \u2014 we know it's a pull_request event\n    body = await request.json()\n\n    action = body.get(\"action\")\n    print(f\"[webhook] PR action received: {action}\")\n\n    if action not in [\"opened\", \"synchronize\"]:\n        return {\"status\": \"ignored\", \"action\": action}\n\n    repo_full_name = body[\"repository\"][\"full_name\"]\n    pr_number = body[\"pull_request\"][\"number\"]\n\n    print(f\"[webhook] Queuing review for PR #{pr_number} in {repo_full_name}\")\n    background_tasks.add_task(review_pr, repo_full_name, pr_number)\n\n    return {\"status\": \"accepted\", \"pr\": pr_number}\n\n\n#"}, {"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\main.py", "start_line": 52, "end_line": 74, "node_type": "function_definition", "content": "c def review_pr(repo_full_name: str, pr_number: int):\n    print(f\"[review_pr] Starting review \u2014 PR #{pr_number} in {repo_full_name}\")\n\n    try:\n        diff = await fetch_pr_diff(repo_full_name, pr_number)\n        print(f\"[review_pr] Diff fetched \u2014 {len(diff)} characters\")\n    except Exception as e:\n        print(f\"[review_pr] FAILED fetch_pr_diff \u2014 PR #{pr_number} \u2014 {e}\")\n        return\n\n    try:\n        review = await generate_code_review(diff)\n        print(f\"[review_pr] Review generated \u2014 {len(review)} characters\")\n    except Exception as e:\n        print(f\"[review_pr] FAILED generate_code_review \u2014 PR #{pr_number} \u2014 {e}\")\n        return\n\n    try:\n        await post_pr_comment(repo_full_name, pr_number, review)\n        print(f\"[review_pr] Done \u2014 comment posted to PR #{pr_number}\")\n    except Exception as e:\n        print(f\"[review_pr] FAILED post_pr_comment \u2014 PR #{pr_number} \u2014 {e}\")\n        return\n\n\n# --- Debug endpoint "}, {"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\main.py", "start_line": 79, "end_line": 84, "node_type": "function_definition", "content": "quest: Request):\n    raw_body = await request.body()\n    print(f\"[debug] content-type: {request.headers.get('content-type')}\")\n    print(f\"[debug] body length: {len(raw_body)}\")\n    print(f\"[debug] raw body: {raw_body[:500]}\")\n    return {\"status\": \"ok\"}"}, {"file_path": "C:\\tmp\\codebase-intelligence\\Dhushyanthc_ai-code-reviewer\\app\\security.py", "start_line": 7, "end_line": 32, "node_type": "function_definition", "content": "async def verify_signature(request: Request):\n  \"\"\"FastAPI dependency runs before the webhook handler.\n  Verifies the request is from the GitHub. \n  Return 401 if the signature is invalid or missing.\"\"\"\n\n  signature_header = request.headers.get(\"X-Hub-Signature-256\")\n\n  if not signature_header:\n    raise HTTPException(\n      status_code=401,\n      detail=\"Missing X-Hub-Signature-256 header\"\n    )\n  \n  body = await request.body()\n\n  expected_signature = \"sha256=\" + hmac.new(\n    key = WEBHOOK_SECRET.encode(\"utf-8\"),\n    msg = body,\n    digestmod = hashlib.sha256\n  ).hexdigest()\n\n  if not hmac.compare_digest(expected_signature, signature_header):\n    HTTPException(\n      status_code=401,\n      detail= \"Invalid signature - request not from GitHub\"\n    )"}]}